import Foundation

/// Top-level namespace for the APX Tester analysis library.
/// All analysis logic (compilation, assembly parsing, pass diffing, CSmith integration)
/// lives in this module so it can be consumed by both the CLI and GUI targets.
public enum APXCore {
    public static let version = "0.1.0"
}
